<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        xmlns:viewModel="clr-namespace:Client.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="1100"
        FocusManager.FocusedElement="{Binding ElementName=MyCanvas}">
    <Canvas Name="MyCanvas" KeyDown="Canvas_KeyDown" KeyUp="MyCanvas_KeyUp" Focusable="True">
        <Rectangle Fill="Red" Canvas.Left="{Binding Game.Square.X}" Canvas.Top="{Binding Game.Square.Y}" Width="{Binding Game.Square.Width}" Height="{Binding Game.Square.Height}" />
    </Canvas>
</Window>
